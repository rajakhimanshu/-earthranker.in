/**
 * LanguageToggle — RETIRED (March 2026)
 * 
 * This component has been superseded by the inline NavLangToggle
 * inside src/components/Navbar.jsx.
 * 
 * It is kept here as a safety stub so that any stale import
 * across the codebase doesn't cause a build error.
 * 
 * DO NOT use this component directly — use Navbar.jsx instead.
 */
export default function LanguageToggle() {
  return null;
}
